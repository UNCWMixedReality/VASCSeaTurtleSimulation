namespace DataCollection.Models
{
    public interface IModel
    {
        /*
        This is just to group all Models by Type for use in parameters
        
        All Models must be json serializable by Newtonsoft.JSON
        
        All current Models are mostly just DataModels storing just data
        
        If a class either has, or is planning to have functionality then it will just be a normal Model
        */
    }
}